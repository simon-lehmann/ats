//! ats-daemon: owns all agent processes (PTYs), workspaces, transcripts,
//! and state. Clients (TUI, CLI) attach over a local socket.

fn main() {
    println!("ats-daemon: not yet implemented (see docs/PLAN.md, Phase 1)");
}
